<%= form_for(@microsites_group) do |f| %>
  <% if @microsites_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@microsites_group.errors.count, "error") %> prohibited this microsites_group from being saved:</h2>

      <ul>
      <% @microsites_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
