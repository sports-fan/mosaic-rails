<h3></h3>
<%#= form_for(@microsite) do |f| %>

<%= form_for micro, :url => "/microsites/#{micro.id}", html: { multipart: true } do |f| %>

  <% if micro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(micro.errors.count, "error") %> prohibited this microsite from being saved:</h2>

      <ul>
      <% micro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 <div class="field">
      <%= hidden_field_tag 'micro', 'true'  %>  
 </div>

  <div class="field">
    <%= f.label :publish %><br>
    <%= f.check_box :publish %>
  </div>
  <div class="actions">
    <%= f.submit "Save" %>
  </div>
<% end %>
