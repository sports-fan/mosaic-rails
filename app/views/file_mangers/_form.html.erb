

   <div class="panel-body">
                               <p id="notice"><%= notice %></p>          
        <%#= form_for @folder, html: { multipart: true, class: "form-horizontal"} do |f| %>
        <%= form_for([@file_manger.folder, @file_manger]) do |f| %>

                                 <% if @file_manger.errors.any? %>
    <div id="error_explanation">
      <h2><%= t('errors.messages.not_saved', count: @file_manger.errors.count, resource: t(:comment)) %></h2>

      <ul>
      <% @file_manger.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
                                    <div class="form-group">
                                                <%= f.label :name, class: "col-md-2 control-label" %>
                                                <div class="col-md-10">
                                              <%= f.text_field :name, class: "form-control" %>
                                                </div>
                                              </div>
 <div class="form-group">
                                                <%= f.label :attachment, class: "col-md-2 control-label" %>
                                                <div class="col-md-10">
    <%= f.file_field :attachment, :required => true %>
                                                </div>
                                              </div>




                                            <div class="form-group m-b-0">
                                                <div class="col-sm-offset-3 col-sm-9">
                                           <%= f.submit t(:Save) , :class=> "btn btn-info waves-effect waves-light" %> 

                                                </div>
                                            </div>

                           
                                       <% end %>

                                    </div> <!-- panel-body -->
                         




