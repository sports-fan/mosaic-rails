<%= form_for(@survey_group) do |f| %>
  <% if @survey_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey_group.errors.count, "error") %> prohibited this survey_group from being saved:</h2>

      <ul>
      <% @survey_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :survey_id %><br>
    <%= f.number_field :survey_id %>
  </div>
  <div class="field">
    <%= f.label :group_id %><br>
    <%= f.number_field :group_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
